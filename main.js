nosex=0;
nosey=0;
difference=0;
rightwristx=0;
leftwristx=0;

function setup(){
    canvas = createCanvas(600,500);
    canvas.position(680,70)
    video = createCapture(VIDEO);
    video.size(600,500);
}